plugins:
   source:
     - module: intake_xarray

sources:
  TRACK:
    args:
      path: "{{CATALOG_DIR}}/track.yaml"
    description: 'Polarstern position during PASCAL'
    driver: yaml_file_cat
    metadata: {}
