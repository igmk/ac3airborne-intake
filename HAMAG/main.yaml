description: AC3airborne Intake Data Catalog
plugins:
  source:
  - module: intake_xarray
sources:
  P6:
    args:
      path: '{{CATALOG_DIR}}/P6/main.yaml'
    description: Data from the Polar 6 aircraft during HAMAG
    driver: yaml_file_cat
    metadata: {}
