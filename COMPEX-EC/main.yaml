description: AC3airborne Intake Data Catalog
plugins:
  source:
  - module: intake_xarray
sources:
  P5:
    args:
      path: '{{CATALOG_DIR}}/P5/main.yaml'
    description: Data from the COMPEX-EC campaign
    driver: yaml_file_cat
    metadata: {}
